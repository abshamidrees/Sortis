import { VerifyScreen } from "@/components/VerifyScreen";

/**
 * app.sortis.xyz/verify
 *
 * Public verifiability is one of the things the bounty names explicitly, and
 * it is the screen most entries will skip.
 */
export default function VerifyPage() {
  return <VerifyScreen />;
}
